// this is a very simple test for adding numbers together
describe("Native addition", function () {
    it("should add the numbers together", function () {
        (1 + 1).should.equal(2);
    });
});

describe("Native subtraction", function () {
    it("should add the numbers together", function () {
        (1 - 1).should.equal(0);
    });
});

